import { FC } from '../../../../node_modules/react';
import { IWatermarkConfig } from '../../../models';

interface Props {
    config: IWatermarkConfig;
}
export declare const WatermarkOverlay: FC<Props>;
export {};
